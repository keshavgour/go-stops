import { StyleSheet } from "react-native";

export const styleReadMore = StyleSheet.create({
    containers:{
        padding :20
    },
    heading:{
        fontSize: 16,
        fontWeight:'bold'
    }
})